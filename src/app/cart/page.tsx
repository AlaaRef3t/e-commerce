import CartTable from '@/components/cart-comps/CartTable';

import React from 'react'


export default function Cart() {

  // const session = useSession();
  // console.log(session);

  // if(!session.data)return <p>You need to log in</p>
  return (
    <CartTable/>
  )
}
